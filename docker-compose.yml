# PHP

phpfpm:
  restart: always
  extends:
    file: php-fpm-7.yml
    service: phpfpm
  links:
    - db:db

# NGINX
nginx:
  restart: always
  image: nginx
  ports:
    - "8000:80"
  links:
    - phpfpm:phpfpm
  volumes:
    - ./nginx/vhost.conf:/etc/nginx/conf.d/default.conf
    - ./app:/var/www/html
    - ./log/nginx:/var/log/nginx

# MYSQL
db:
  restart: always
  image: mysql
  ports:
    - "3306:3306"
  environment:
    MYSQL_ROOT_PASSWORD: 8mSlzaHN2WXbkIGHnOjJ
    MYSQL_USER: semerik 
    MYSQL_PASSWORD: UC1BZXBIEANwaUS4i8kh
    MYSQL_DATABASE: semerik
  volumes:
    - ./mysql/databases:/var/lib/mysql
    - ./log/mysql:/var/log/mysql

#  - /var/run/mysqld/mysqld.sock:/var/run/mysqld/mysqld.sock
